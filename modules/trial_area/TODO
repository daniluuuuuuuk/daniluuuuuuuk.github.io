Evaluation&Restatement:
# Окно ошибки (страница 640 книги)
# Править пути (взять с конфига)

Evaluation:
# Не импортируется перечётка в форму. Хотя в restatement данные есть
# В методе render_wpulp_table изменить конвертацию кода породы в породу:
# Species.select().where(Species.code_species == rec['code_species']).get().name_species  >>  Species.get(Species.code_species == rec['code_species']).name_species
# Trf_height.get(Trf_height.code_trf_height == rec['code_trf_height']).name_trf_height
Не берёт в расчёт:
Не считает стоимость (или не правильно считает):
- Ликвид из кроны
- Хворост
- Отходы
Не считает по таблицам (Сортиментные таблицы для промежуточного пользования. Приказ МЛХ №175 от 28.10.2016г.)
Не считает скидки

Restatement: